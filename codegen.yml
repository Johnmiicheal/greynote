overwrite: true
# schema: "https://api.greynote.app/graphql"
schema: "http://localhost:4000/graphql"
documents: "src/graphql/**/*.graphql"
generates:
  src/gql/graphql.ts:
    # preset: "client"
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
