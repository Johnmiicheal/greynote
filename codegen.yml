overwrite: true
schema: "https://api.graybook.app/graphql"
documents: "src/graphql/**/*.graphql"
generates:
  src/gql/graphql.ts:
    # preset: "client"
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
